<nav class="navbar navbar-expand-sm navbar-light navbar-lewagon d-none d-md-flex">
  <div class="container-fluid">
    <%= render "shared/navbar/logo" %>

    <h2 class="flex-grow-1 text-center m-0">
      <%= link_to "StoryPile", root_path, class: "navbar-title" %>
    </h2>

    <ul class="navbar-nav">
      <li class="nav-item dropdown">
        <a href="#" class="nav-link dropdown-toggle-no-caret"
            id="desktopMenuDropdown" role="button" data-bs-toggle="dropdown">
          <i class="fa-solid fa-circle-user fa-2xl" style="position: relative; top: 8px;"></i>
        </a>

        <ul class="dropdown-menu dropdown-menu-end" aria-labelledby="desktopMenuDropdown">
          <%= render "shared/navbar/nav_links" %>
          <%= render "shared/navbar/account_links" %>
          <%= render "shared/navbar/install_button", id: "install-menu-item-desktop", button_id: "install-button-desktop" %>
        </ul>
      </li>
    </ul>
  </div>
</nav>
