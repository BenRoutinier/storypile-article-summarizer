<div class="container my-4">
  <h1><%= @curation.title %></h1>

  <div class="row g-3">
    <%= render "shared/manyarticles", articles: @articles, curation: @curation %>
  </div>

  <% if @articles.empty? %>
    <p class="text-center text-muted">No articles in this curation yet.</p>
  <% end %>
</div>
